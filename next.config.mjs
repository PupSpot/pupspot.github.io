/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true, //using server side rendering
  // output: "export",
  images: { unoptimized: true }
};

export default nextConfig;
